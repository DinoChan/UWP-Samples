<UserControl x:Class="ShapeDemoSilverlight.MainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400"
             xmlns:local="clr-namespace:ShapeDemoSilverlight">
    <UserControl.Resources>
        <Storyboard x:Name="Storyboard1"
                   >
            <DoubleAnimation Duration="0:1:1"
                             To="-500"
                             Storyboard.TargetProperty="(Shape.StrokeDashOffset)"
                             Storyboard.TargetName="triangle"
                             d:IsOptimized="True" />
        </Storyboard>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot"
          Background="White">
        <local:Triangle x:Name="triangle"
                        Height="40"
                        Width="40"
                        StrokeThickness="2"
                        Stroke="RoyalBlue" 
                        StrokeDashArray="4.045 4.045" StrokeDashOffset="0.05"
                        StrokeDashCap="Round" />
    </Grid>
</UserControl>
